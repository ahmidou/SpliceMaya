global proc fabricSaveDFG()
{
	string $selNodes[] = `ls -sl -et "dfgMayaNode"`;
	if(size($selNodes) > 0)
		dfgExportJSON -n $selNodes[0] -f "";
}

global proc fabricLoadDFG()
{
	string $selNodes[] = `ls -sl -et "dfgMayaNode"`;
	if(size($selNodes) > 0)
		dfgImportJSON -n $selNodes[0] -f "";
}
